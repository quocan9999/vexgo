'use client';

import { useState } from 'react';
import Link from 'next/link';
import { AlertTriangle, CheckCircle2, ChevronLeft, TicketX } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { getTicket } from '@/mocks/tickets';
import { formatCompactCurrency } from '@/lib/format';

export default function CancelTicketPage() {
  const [step, setStep] = useState<1 | 2 | 3>(1);
  const ticket = getTicket('ticket-001');
  return <div className="mx-auto max-w-2xl px-4 py-10 sm:px-0"><Link href={`/invoices/${ticket.id}`} className="inline-flex items-center gap-2 text-sm font-bold text-muted-foreground hover:text-primary"><ChevronLeft size={16} /> Quay lại vé</Link><div className="mt-6 rounded-3xl border border-border bg-background p-5 shadow-sm sm:p-8"><div className="flex items-center justify-between"><div><p className="text-xs font-black uppercase tracking-wider text-accent">Hủy vé</p><h1 className="mt-2 text-2xl font-black">Yêu cầu hủy vé {ticket.code}</h1></div><span className="grid size-12 place-items-center rounded-2xl bg-rose-50 text-rose-600"><TicketX /></span></div><div className="mt-8 flex items-center gap-2 text-xs font-black"><span className={`rounded-full px-3 py-2 ${step >= 1 ? 'bg-primary text-primary-foreground' : 'bg-muted text-muted-foreground'}`}>1. Kiểm tra</span><span className="h-px flex-1 bg-border" /><span className={`rounded-full px-3 py-2 ${step >= 2 ? 'bg-primary text-primary-foreground' : 'bg-muted text-muted-foreground'}`}>2. Xác nhận</span><span className="h-px flex-1 bg-border" /><span className={`rounded-full px-3 py-2 ${step >= 3 ? 'bg-primary text-primary-foreground' : 'bg-muted text-muted-foreground'}`}>3. Hoàn tất</span></div>{step === 1 ? <div className="mt-8 space-y-5"><div className="rounded-2xl bg-muted p-4"><p className="font-black">{ticket.route}</p><p className="mt-1 text-sm text-muted-foreground">{ticket.departure} · Ghế {ticket.seats.join(', ')}</p></div><div className="flex gap-3 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-800"><AlertTriangle size={20} className="mt-0.5 shrink-0" /><span>Phí hủy và số tiền hoàn thực tế sẽ do backend trả theo chính sách áp dụng cho vé.</span></div><Button variant="danger" className="w-full" onClick={() => setStep(2)}>Tiếp tục hủy vé</Button></div> : null}{step === 2 ? <div className="mt-8 space-y-5"><div className="rounded-2xl border border-border p-4 text-sm"><div className="flex justify-between"><span className="text-muted-foreground">Giá trị vé</span><b>{formatCompactCurrency(ticket.amount)}</b></div><div className="mt-3 flex justify-between"><span className="text-muted-foreground">Hoàn dự kiến</span><b className="text-primary">Theo xác nhận backend</b></div></div><label className="flex items-start gap-3 rounded-2xl bg-muted p-4 text-sm font-semibold"><input type="checkbox" className="mt-1 accent-primary" /> Tôi đã đọc và đồng ý với chính sách hủy vé.</label><div className="flex gap-3"><Button variant="secondary" className="flex-1" onClick={() => setStep(1)}>Quay lại</Button><Button variant="danger" className="flex-1" onClick={() => setStep(3)}>Xác nhận hủy</Button></div></div> : null}{step === 3 ? <div className="mt-10 text-center"><div className="mx-auto grid size-16 place-items-center rounded-full bg-emerald-50 text-emerald-600"><CheckCircle2 size={34} /></div><h2 className="mt-5 text-2xl font-black">Đã ghi nhận yêu cầu demo</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Trạng thái hoàn tiền sẽ được cập nhật từ backend khi tích hợp nghiệp vụ VexGo.</p><Link href="/account/tickets" className="mt-7 inline-flex min-h-11 items-center rounded-xl bg-primary px-5 text-sm font-bold text-primary-foreground">Về vé của tôi</Link></div> : null}</div></div>;
}
