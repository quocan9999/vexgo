import Link from 'next/link';
import { CheckCircle2, Download, Mail, MapPin, QrCode } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { getTicket } from '@/mocks/tickets';
import { formatCompactCurrency } from '@/lib/format';

export default async function InvoicePage({ params }: { params: Promise<{ invoiceId: string }> }) {
  const { invoiceId } = await params;
  const ticket = getTicket(invoiceId);
  return <div className="bg-slate-50 px-4 py-10 sm:px-6 lg:px-8"><div className="mx-auto max-w-4xl"><div className="mb-6 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><Link href="/" className="text-sm font-bold text-muted-foreground hover:text-primary">← Về trang chủ</Link><div className="flex gap-2"><Button variant="secondary"><Mail size={16} /> Gửi email</Button><Button variant="primary"><Download size={16} /> Tải PDF</Button></div></div><div className="overflow-hidden rounded-3xl border border-border bg-background shadow-sm"><div className="grid md:grid-cols-[1fr_320px]"><div className="p-6 sm:p-10"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[0.2em] text-primary">VexGo Transport</p><h1 className="mt-3 text-3xl font-black">Vé điện tử</h1><p className="mt-1 text-sm font-bold text-muted-foreground">Mã vé: {ticket.code}</p></div><span className="flex items-center gap-1.5 rounded-xl border border-emerald-200 bg-emerald-50 px-3 py-2 text-xs font-black text-emerald-700"><CheckCircle2 size={16} /> Đã xác nhận</span></div><div className="mt-10 rounded-2xl bg-muted p-5"><p className="text-xs font-bold text-muted-foreground">Hành trình</p><h2 className="mt-2 text-2xl font-black">{ticket.route}</h2><p className="mt-2 text-sm font-semibold text-muted-foreground">{ticket.vehicleType}</p></div><div className="mt-8 grid gap-6 sm:grid-cols-2"><div><p className="text-xs font-bold text-muted-foreground">Khách hàng</p><p className="mt-1 font-black">{ticket.passenger}</p></div><div><p className="text-xs font-bold text-muted-foreground">Số điện thoại</p><p className="mt-1 font-black">{ticket.phone}</p></div><div><p className="text-xs font-bold text-muted-foreground">Thời gian đi</p><p className="mt-1 font-black">{ticket.departure}</p></div><div><p className="text-xs font-bold text-muted-foreground">Số ghế</p><p className="mt-1 font-black">{ticket.seats.join(', ')}</p></div><div className="sm:col-span-2"><p className="text-xs font-bold text-muted-foreground">Điểm đón / trả</p><p className="mt-1 flex items-center gap-2 font-black"><MapPin size={16} className="text-primary" /> {ticket.pickup} → {ticket.dropoff}</p></div></div></div><div className="flex flex-col items-center justify-center border-t border-dashed border-border p-8 md:border-l md:border-t-0"><p className="text-xs font-black uppercase tracking-wider text-muted-foreground">Mã đặt chỗ</p><p className="mt-2 text-2xl font-black tracking-widest">{ticket.code}</p><div className="mt-7 grid size-48 place-items-center rounded-3xl border border-border bg-background shadow-sm"><QrCode size={150} strokeWidth={1.2} /></div><p className="mt-6 text-center text-sm font-semibold leading-6 text-muted-foreground">Vui lòng xuất trình mã QR khi lên xe.</p><p className="mt-5 text-lg font-black text-primary">{formatCompactCurrency(ticket.amount)}</p></div></div></div><div className="mt-5 text-center text-xs font-semibold text-muted-foreground">Giao diện hóa đơn demo; tải file và gửi email sẽ được nối sau khi có backend.</div></div></div>;
}
